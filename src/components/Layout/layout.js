import { Header } from '../Header';
import { Footer } from '../Footer';
import { MainSection } from '../MainSection';

export const Layout = () => {
  return (
    <>
    <Header />
    <MainSection />
    <Footer />
    </>
  );
}